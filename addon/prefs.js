/* eslint-disable no-undef */
// No default prefs are declared: every setting is read with an in-code fallback
// (see prefs() in src/modules/chatPanel.ts), e.g.
//   extensions.paper-reading-agent.codexPath        (default: search PATH)
//   extensions.paper-reading-agent.model            (default: codex config)
//   extensions.paper-reading-agent.reasoningEffort  (default: codex default)
//   extensions.paper-reading-agent.timeoutSec       (default: 600)
//   extensions.paper-reading-agent.webSearch        (default: true)
// Set them via Settings → Advanced → Config Editor.
